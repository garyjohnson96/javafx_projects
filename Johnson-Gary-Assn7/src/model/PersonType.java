package model;

/**
 * A Functional Interface that provides a single abstract method for accessing a person's type.
 * @author Gary Johnson
 * @version 1.0
 */
interface PersonType {
    /**
     * An abstract method used to get a person's type.
     * @return the person's specified type
     */
    String getPersonType();
}
